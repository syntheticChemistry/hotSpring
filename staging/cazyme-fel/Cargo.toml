[package]
name = "cazyme-fel"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "CAZyme conformational FEL validation module for lithoSpore"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
approx = "0.5"
