# SPDX-License-Identifier: AGPL-3.0-or-later
#
# hotSpring Capability Registry
#
# Authoritative list of capabilities this spring exposes. Bidirectional
# sync tested against niche.rs (LOCAL_CAPABILITIES + ROUTED_CAPABILITIES).
# When adding a capability, add it here AND in niche.rs — the test
# `capabilities_match_registry_toml` enforces lockstep.
#
# Schema: each [[capabilities]] entry has:
#   method   — fully qualified domain.operation name
#   served   — "local" (hotspring_primal handles) or "routed" (proxy to provider)
#   provider — canonical primal for routed capabilities (empty for local)
#   domain   — top-level domain grouping

# ── Physics domain (locally served) ──────────────────────────────────

[[capabilities]]
method = "physics.lattice_qcd"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.lattice_gauge_update"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.hmc_trajectory"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.wilson_dirac"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.molecular_dynamics"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.fluid"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.nuclear_eos"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.thermal"
served = "local"
provider = ""
domain = "physics"

[[capabilities]]
method = "physics.radiation"
served = "local"
provider = ""
domain = "physics"

# ── Compute primitives (locally served) ──────────────────────────────

[[capabilities]]
method = "compute.df64"
served = "local"
provider = ""
domain = "compute"

[[capabilities]]
method = "compute.cg_solver"
served = "local"
provider = ""
domain = "compute"

[[capabilities]]
method = "compute.gradient_flow"
served = "local"
provider = ""
domain = "compute"

[[capabilities]]
method = "compute.f64"
served = "local"
provider = ""
domain = "compute"

# ── Composition health (locally served) ──────────────────────────────

[[capabilities]]
method = "composition.health"
served = "local"
provider = ""
domain = "composition"

# ── Lifecycle / health (locally served) ──────────────────────────────

[[capabilities]]
method = "health.check"
served = "local"
provider = ""
domain = "health"

[[capabilities]]
method = "health.liveness"
served = "local"
provider = ""
domain = "health"

[[capabilities]]
method = "health.readiness"
served = "local"
provider = ""
domain = "health"

[[capabilities]]
method = "capabilities.list"
served = "local"
provider = ""
domain = "capabilities"

[[capabilities]]
method = "mcp.tools.list"
served = "local"
provider = ""
domain = "mcp"

# ── Inference (routed to Squirrel -> neuralSpring) ───────────────────

[[capabilities]]
method = "inference.complete"
served = "routed"
provider = "squirrel"
domain = "inference"

[[capabilities]]
method = "inference.embed"
served = "routed"
provider = "squirrel"
domain = "inference"

[[capabilities]]
method = "inference.models"
served = "routed"
provider = "squirrel"
domain = "inference"

# ── Crypto (routed to BearDog) ───────────────────────────────────────

[[capabilities]]
method = "crypto.sign_ed25519"
served = "routed"
provider = "beardog"
domain = "crypto"

[[capabilities]]
method = "crypto.verify_ed25519"
served = "routed"
provider = "beardog"
domain = "crypto"

# ── Compute dispatch (routed to ToadStool) ───────────────────────────

[[capabilities]]
method = "compute.dispatch.submit"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.dispatch.capabilities"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.dispatch.result"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.dispatch.status"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.dispatch.forward"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Tier 2 Live Science API (routed to ToadStool) ────────────────────

[[capabilities]]
method = "toadstool.validate"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "toadstool.list_workloads"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Ember device listing (toadStool Phase A/B — operational) ──────────

[[capabilities]]
method = "ember.status"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.list"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.reacquire"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Device management (toadStool Phase C — operational S252+) ─────────

[[capabilities]]
method = "device.list"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.status"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.reacquire"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.swap"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.warm_catch"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── VFIO PBDMA dispatch (toadStool S258) ──────────────────────────────

[[capabilities]]
method = "device.vfio.open"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.vfio.alloc"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "device.vfio.roundtrip"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── MMIO (toadStool S252 — hardware register access) ─────────────────

[[capabilities]]
method = "mmio.read32"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "mmio.write32"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "mmio.batch"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "mmio.pramin.read32"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "mmio.bar0.probe"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "mmio.falcon.status"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Ember lifecycle (toadStool Phase D — NVIDIA FECS-gated) ───────────
# These methods exist as types/traits but NVIDIA dispatch returns
# Unsupported until FECS firmware bridge is resolved. AMD path is live.

[[capabilities]]
method = "ember.warm_cycle"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.adopt_device"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.fecs.state"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.device.health"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "ember.device.recover"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Semantic aliases (toadStool S255 — backward compat) ──────────────
# ember.swap and sovereign.boot are semantic aliases to device.swap
# in toadStool. hotSpring GlowplugClient uses these names.

[[capabilities]]
method = "ember.swap"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "sovereign.boot"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── GPU introspection (toadStool — operational) ───────────────────────

[[capabilities]]
method = "gpu.query_info"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "gpu.query_memory"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "gpu.query_telemetry"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Hardware learning (toadStool — operational) ───────────────────────

[[capabilities]]
method = "compute.hardware.observe"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.status"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.vfio_devices"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Dispatch pipeline (toadStool — multi-stage ordered dispatch) ──────

[[capabilities]]
method = "compute.dispatch.pipeline.submit"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.dispatch.pipeline.status"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Performance surface + multi-unit routing (toadStool — operational) ─

[[capabilities]]
method = "compute.performance_surface.report"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.performance_surface.query"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.performance_surface.list"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.route.multi_unit"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Hardware learning expanded (toadStool S237+ — operational) ────────

[[capabilities]]
method = "compute.hardware.distill"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.apply"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.share_recipe"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.auto_init"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "compute.hardware.auto_init_all"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Shader dispatch (toadStool — accepts compiled binaries) ───────────

[[capabilities]]
method = "shader.dispatch"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Auth (toadStool MethodGate — operational) ─────────────────────────

[[capabilities]]
method = "auth.check"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "auth.mode"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "auth.peer_info"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Provenance (toadStool — operational) ──────────────────────────────

[[capabilities]]
method = "provenance.query"
served = "routed"
provider = "toadstool"
domain = "compute"

[[capabilities]]
method = "provenance.get"
served = "routed"
provider = "toadstool"
domain = "compute"

# ── Precision advisory (routed to barraCuda) ─────────────────────────

[[capabilities]]
method = "precision.route"
served = "routed"
provider = "barracuda"
domain = "math"

# ── Shader compilation (routed to coralReef) ─────────────────────────

[[capabilities]]
method = "shader.compile.wgsl"
served = "routed"
provider = "coralreef"
domain = "shader"

[[capabilities]]
method = "shader.compile.spirv"
served = "routed"
provider = "coralreef"
domain = "shader"

[[capabilities]]
method = "shader.compile.wgsl.multi"
served = "routed"
provider = "coralreef"
domain = "shader"

[[capabilities]]
method = "shader.compile.gemm"
served = "routed"
provider = "coralreef"
domain = "shader"

[[capabilities]]
method = "shader.compile.status"
served = "routed"
provider = "coralreef"
domain = "shader"

[[capabilities]]
method = "shader.compile.capabilities"
served = "routed"
provider = "coralreef"
domain = "shader"

# ── Storage (routed to NestGate) ─────────────────────────────────────

[[capabilities]]
method = "storage.store"
served = "routed"
provider = "nestgate"
domain = "storage"

[[capabilities]]
method = "storage.retrieve"
served = "routed"
provider = "nestgate"
domain = "storage"

[[capabilities]]
method = "storage.list"
served = "routed"
provider = "nestgate"
domain = "storage"

# ── Provenance DAG (routed to rhizoCrypt) ────────────────────────────

[[capabilities]]
method = "dag.session.create"
served = "routed"
provider = "rhizocrypt"
domain = "dag"

[[capabilities]]
method = "dag.event.append"
served = "routed"
provider = "rhizocrypt"
domain = "dag"

[[capabilities]]
method = "dag.merkle.root"
served = "routed"
provider = "rhizocrypt"
domain = "dag"

[[capabilities]]
method = "dag.merkle.verify"
served = "routed"
provider = "rhizocrypt"
domain = "dag"

# ── Ledger (routed to loamSpine) ─────────────────────────────────────

[[capabilities]]
method = "spine.create"
served = "routed"
provider = "loamspine"
domain = "ledger"

[[capabilities]]
method = "entry.append"
served = "routed"
provider = "loamspine"
domain = "ledger"

[[capabilities]]
method = "session.commit"
served = "routed"
provider = "loamspine"
domain = "ledger"

[[capabilities]]
method = "certificate.mint"
served = "routed"
provider = "loamspine"
domain = "ledger"

# ── Attribution (routed to sweetGrass) ───────────────────────────────

[[capabilities]]
method = "braid.create"
served = "routed"
provider = "sweetgrass"
domain = "attribution"

[[capabilities]]
method = "braid.commit"
served = "routed"
provider = "sweetgrass"
domain = "attribution"

[[capabilities]]
method = "provenance.graph"
served = "routed"
provider = "sweetgrass"
domain = "attribution"

[[capabilities]]
method = "provenance.export_provo"
served = "routed"
provider = "sweetgrass"
domain = "attribution"

[[capabilities]]
method = "attribution.chain"
served = "routed"
provider = "sweetgrass"
domain = "attribution"

# ── Discovery (routed to Songbird) ───────────────────────────────────

[[capabilities]]
method = "discovery.find_primals"
served = "routed"
provider = "songbird"
domain = "discovery"

[[capabilities]]
method = "discovery.announce"
served = "routed"
provider = "songbird"
domain = "discovery"

# ── Visualization + Interaction (routed to petalTongue) ───────────────

[[capabilities]]
method = "visualization.render"
served = "routed"
provider = "petaltongue"
domain = "visualization"

[[capabilities]]
method = "visualization.render.scene"
served = "routed"
provider = "petaltongue"
domain = "visualization"

[[capabilities]]
method = "visualization.render.stream"
served = "routed"
provider = "petaltongue"
domain = "visualization"

[[capabilities]]
method = "interaction.subscribe"
served = "routed"
provider = "petaltongue"
domain = "visualization"

[[capabilities]]
method = "interaction.poll"
served = "routed"
provider = "petaltongue"
domain = "visualization"
