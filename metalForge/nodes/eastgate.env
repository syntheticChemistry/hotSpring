# Eastgate — Utility Compute + Neuromorphic
# i9-12900, 32GB DDR5, RTX 4070 + Titan V, 1x Akida NPU
#
# Usage:
#   source metalForge/nodes/eastgate.env
#   cargo run --release --bin <job>

# Primary GPU: RTX 4070 (12 GB, Ada Lovelace AD104, nvidia proprietary)
HOTSPRING_GPU_ADAPTER=4070
HOTSPRING_WGPU_BACKEND=vulkan

# Multi-GPU: primary compute + NVK verification
HOTSPRING_GPU_PRIMARY=4070
HOTSPRING_GPU_SECONDARY=titan
