# biomeGate — Semi-Mobile Mini HPC
# Threadripper 3970X, 256GB DDR4, RTX 3090 + Titan V, 1x Akida NPU
#
# Usage:
#   source metalForge/nodes/biomegate.env
#   cargo run --release --bin <job>
#
# Setup notes (first boot):
#   1. The toadstool dependency uses a case-sensitive path.  Ensure the
#      lowercase symlink exists:
#        ln -s ~/Development/ecoPrimals/phase1/toadStool \
#               ~/Development/ecoPrimals/phase1/toadstool
#   2. Use the explicit toolchain path to avoid Cursor IDE proxy conflicts:
#        CARGO=~/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/bin/cargo
#        RUSTC=~/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/bin/rustc
#      Both are set below for `cargo build` / `cargo run` calls.

# Primary GPU: RTX 3090 (24 GB, Ampere GA102, nvidia proprietary)
HOTSPRING_GPU_ADAPTER=3090
HOTSPRING_WGPU_BACKEND=vulkan

# Multi-GPU: primary compute + verification/secondary
HOTSPRING_GPU_PRIMARY=3090
HOTSPRING_GPU_SECONDARY=titan

# Scratch space (2x 2TB NVMe work drives)
HOTSPRING_SCRATCH=/mnt/scratch

# Rust toolchain — explicit paths prevent Cursor IDE argv[0] proxy conflict
RUSTUP_TOOLCHAIN=stable
RUSTC=~/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/bin/rustc
CARGO=~/.rustup/toolchains/stable-x86_64-unknown-linux-gnu/bin/cargo
